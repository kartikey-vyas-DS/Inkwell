# Inkwell — Python Dependencies
# Install with: pip install -r requirements.txt

# Core
anthropic>=0.40.0
voyageai>=0.3.0
chromadb>=0.5.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9

# PDF processing
pymupdf>=1.24.0
pdfplumber>=0.11.0

# Search & retrieval
rank-bm25>=0.2.2

# Environment
python-dotenv>=1.0.0

# Optional — only needed if using Gemini vision or Gemini as synthesis model
google-generativeai>=0.8.0
Pillow>=10.0.0

# Optional — only needed if using GPT or DeepSeek models
openai>=1.0.0
